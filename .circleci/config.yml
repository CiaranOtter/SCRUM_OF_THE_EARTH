version: 2.1

orbs:
  coveralls: coveralls/coveralls@1.0.6
  node: circleci/node@5.0.2

jobs:
  test-build:
    executor:
      name: node/default

    steps:
      - checkout

      - run: npm install
      - run: npm run expo-test
      - run: npm run build

  test-Coverage:
    executor:
      name: node/default
      tag: '15.0'm ]OKpki998iujkM  09iok ,

    steps:
      - checkout

      - node/with-cache:
          steps:
            - run: npm install
            - run: npm install coveralls --save-dev
            # - run: npm run coveralls

      - coveralls/upload:
          parallel: true
          flag_name: Test 1
          verbose: true

  done:
    executor:
      name: node/default

    steps:
      - coveralls/upload:
          parallel_finished: true

workflows:
  test_parallel_then_upload:
    jobs:
      - test-build
      - test-Coverage
          require: [test-build]
      - done:
          requires: [test-Coverage, test-build]
