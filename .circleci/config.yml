version: 2.1



orbs: 
  coveralls: coveralls/coveralls@1.0.10

jobs:
  say-hello:
    working_directory: ~/project/Keep_Track

    docker:
      - image: cimg/node:16.13.2
    steps:
      - checkout
      - run: ls; pwd;

          
  install-node-modules:
    build:
    docker:
      - image: cimg/node:16.13.2

    repo_token: uBUJ6eadGjnX1wRxPovjb7PqxLicixf5B

    steps: 
      - checkout

      - run: 
          name: "List files"
          command: "ls;"

      - run: 
          name: "Intsall npm modules"
          command: "cd musician; npm install; npm install jest"

      - run: 
          name: "Run tests"
          command: "cd musician; npm test"
      
      - coveralls/upload


workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - install-node-modules
